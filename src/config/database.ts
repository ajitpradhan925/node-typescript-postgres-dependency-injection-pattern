const databaseConfig = {
    database: 'test_db',
    username: 'postgres',
    password: 'root',
    host: 'localhost',
    dialect: 'postgres',
  };
  // POSTGRES_URL=postgres://postgres:root@localhost:5432/mrms
  export default databaseConfig;